//Parameters: a whole number, positive or negative
//Return: the given number, but the opposite polarity

function opposite(number) {
    //convert number and return value
    return number - number * 2
  }