//Parameters: a position, and a dice roll
//Return: the position plus the dice roll * 2

//calculate the current position plus 2 dice rolls and return
const move = (position, roll) => position + roll * 2